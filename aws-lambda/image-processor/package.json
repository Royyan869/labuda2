{
  "name": "labuda-image-processor",
  "version": "1.0.0",
  "description": "AWS Lambda function for processing images uploaded to S3",
  "main": "index.js",
  "dependencies": {
    "aws-sdk": "^2.1450.0",
    "sharp": "^0.32.6",
    "blurhash": "^2.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npm ci --production",
    "package": "zip -r function.zip ."
  }
}